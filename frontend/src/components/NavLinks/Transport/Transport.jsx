import React from 'react';
import MapComponent from './MapComponent'; // Assuming MapComponent is in the same directory

const Transport = () => {
  return (
    <div>
      <h1>Transport Component</h1>
      <MapComponent />
    </div>
  );
};

export default Transport;
